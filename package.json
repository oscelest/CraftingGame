{
  "name": "electron-next-skeleton",
  "productName": "ElectronNext",
  "version": "1.0.0",
  "main": "main/index.js",
  "scripts": {
    "build-main": "tsc -p ./main/tsconfig.json",
    "build-renderer": "tsc -p ./renderer/tsconfig.json",
    "start": "electron .",
    "build": "next build renderer && next export renderer",
    "dist": "npm run build && build --dir"
  },
  "build": {
    "asar": false,
    "extraResources": [
      {
        "from": "renderer/out",
        "to": "app/renderer"
      }
    ]
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "electron": "^5.0.3",
    "electron-builder": "^20.39.0",
    "next": "^8.1.0",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "typescript": "^3.5.1",
    "@types/react": "^16.8.20"
  },
  "dependencies": {
    "electron-is-dev": "^1.0.1",
    "electron-next": "^3.1.5"
  }
}
